name: C/C++ CI

on:
  push:
    branches: [ Test-actions ]
  pull_request:
    branches: [ development ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: debug
        run: env
      - name: Boost
        run: echo '::set-env name=BOOST_ROOT::$BOOST_ROOT_1_72_0' && echo $BOOST_ROOT
      - name: make
        run: make
